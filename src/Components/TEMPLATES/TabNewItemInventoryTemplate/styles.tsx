import styled from 'styled-components/native'
import { Box, View } from 'native-base'

export const BoxNewItem = styled(Box)`
  height: 60%;
  flex: 1;
  padding: 5px;
  gap: 16px;
  margin-bottom: 8px;
`
export const WrapperNewitemButtons = styled(View)`
  gap: 8px;
`
